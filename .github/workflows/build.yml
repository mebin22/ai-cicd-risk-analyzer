jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Demo high-risk failure
        run: |
          echo "Unit test failed"
          echo "Docker build error"
          echo "Deployment failed"
          exit 1